<nav class='navbar navbar-inverse navbar-consumer navbar-fixed-top'>
  <div class='container-fluid'>
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class='navbar-header'>
      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#bs-example-navbar-collapse-1'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>

      </button>
      <%= link_to root_path do %>
        <%= image_tag 'images.jpeg', width: 50 %>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class='collapse navbar-collapse' id='bs-example-navbar-collapse-1'>
      <ul class='nav navbar-nav'>
        <% if current_user.is_a? Pro %>
          <li><%= link_to(fa_icon('bar-chart-o', text: 'Dashboard'), dashboard_path) %></li>
        <% end %>
        <% if current_user.is_a? Consumer %> 
          <li>            
            <%= link_to cart_path do%>
              <i class="fa fa-shopping-cart"></i>
              My Cart (<span class="cart-count"><%= shopping_cart_count %></span>)
            <%end%>
          </li>
          <li>            
            <%= link_to transactions_path do%>
              <i class="fa fa-list"></i>
              Bookings History
            <%end%>
          </li>
          <li>            
            <%= check_balance_path %>
          </li>          
        <%end%>
      </ul>
      <ul class='nav navbar-nav navbar-right'>
        <% if current_user %>
          <li class='dropdown login_logout_link'>
            <a href='#' class='dropdown-toggle' data-toggle='dropdown' role='button' aria-expanded='false'>

              <%= fa_icon('user', text: login_name) %>
              <span class='caret'></span>
            </a>
            <ul class='dropdown-menu' role='menu'>
              <% if current_user.is_a? Consumer %>
                <li><%= link_to(fa_icon('cogs', text: 'Account details'), edit_consumer_registration_path) %></li>
              <% end %>
              <li><%= link_to(fa_icon('sign-out', text: 'Logout'), destroy_pro_session_path, method: :delete) %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to(fa_icon('sign-in', text: 'Login'), new_pro_session_path) %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
